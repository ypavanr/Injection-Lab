{
  "name": "@vulncms/database",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:seed": "DOTENV_CONFIG_PATH=../../.env ts-node -r dotenv/config prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.12.0"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "prisma": "^5.12.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
